import { StyleSheet } from 'react-native'

const Styles = StyleSheet.create({
  text: {
    fontSize: 20,
    letterSpacing: 0.05,
    color: "#000000"
  },
  constainer: { 
    flex: 1,
    margin: 20
  }
})

export default Styles